# Backend API. Matches the FastAPI service in ../backend (uvicorn default port).
# Its CORS_ORIGINS already allows http://localhost:5173.
VITE_API_URL=http://localhost:8000

# Live case notifications. Endpoint is /ws on the same service.
VITE_WS_URL=ws://localhost:8000/ws
